import { DescriptionValueObject } from '.';

describe('DescriptionValueObject', () => {
  let descriptionValueObject: DescriptionValueObject;

  beforeEach(() => {
    descriptionValueObject = new DescriptionValueObject();
  });

  it('should be defined', () => {
    expect(descriptionValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de DescriptionValueObject
});
