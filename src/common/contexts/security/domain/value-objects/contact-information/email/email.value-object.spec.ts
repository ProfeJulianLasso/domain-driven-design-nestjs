import { EmailValueObject } from '.';

describe('EmailValueObject', () => {
  let emailValueObject: EmailValueObject;

  beforeEach(() => {
    emailValueObject = new EmailValueObject();
  });

  it('should be defined', () => {
    expect(emailValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de emailValueObject
});
