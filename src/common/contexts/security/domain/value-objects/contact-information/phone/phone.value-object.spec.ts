import { PhoneValueObject } from '.';

describe('PhoneValueObject', () => {
  let phoneValueObject: PhoneValueObject;

  beforeEach(() => {
    phoneValueObject = new PhoneValueObject();
  });

  it('should be defined', () => {
    expect(phoneValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de phoneValueObject
});
