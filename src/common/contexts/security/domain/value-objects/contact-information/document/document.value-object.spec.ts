import { DocumentValueObject } from '.';

describe('DocumentValueObject', () => {
  let documentValueObject: DocumentValueObject;

  beforeEach(() => {
    documentValueObject = new DocumentValueObject();
  });

  it('should be defined', () => {
    expect(documentValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de documentValueObject
});
