import { ContextObjectValue } from '.';

describe('ContextObjectValue', () => {
  let contextObjectValue: ContextObjectValue;

  beforeEach(() => {
    contextObjectValue = new ContextObjectValue();
  });

  it('should be defined', () => {
    expect(contextObjectValue).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de ContextObjectValue
});
