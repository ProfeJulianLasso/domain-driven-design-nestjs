import { DateTimeEndObjectValue } from '.';

describe('DateTimeEndObjectValue', () => {
  let dateTimeEndObjectValue: DateTimeEndObjectValue;

  beforeEach(() => {
    dateTimeEndObjectValue = new DateTimeEndObjectValue();
  });

  it('should be defined', () => {
    expect(dateTimeEndObjectValue).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de DateTimeEndObjectValue
});
