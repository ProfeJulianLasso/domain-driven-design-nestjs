import { DateTimeInitObjectValue } from '.';

describe('DateTimeInitObjectValue', () => {
  let dateTimeInitObjectValue: DateTimeInitObjectValue;

  beforeEach(() => {
    dateTimeInitObjectValue = new DateTimeInitObjectValue();
  });

  it('should be defined', () => {
    expect(dateTimeInitObjectValue).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de DateTimeInitObjectValue
});
