import { ContextValueObject } from '.';

describe('ContextValueObject', () => {
  let contextValueObject: ContextValueObject;

  beforeEach(() => {
    contextValueObject = new ContextValueObject();
  });

  it('should be defined', () => {
    expect(contextValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de ContextValueObject
});
