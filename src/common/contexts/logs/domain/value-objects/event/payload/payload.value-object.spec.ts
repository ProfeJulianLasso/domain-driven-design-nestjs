import { PayloadValueObject } from '.';

describe('PayloadValueObject', () => {
  let payloadValueObject: PayloadValueObject;

  beforeEach(() => {
    payloadValueObject = new PayloadValueObject();
  });

  it('should be defined', () => {
    expect(payloadValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de PayloadValueObject
});
