import { PageValueObject } from '.';

describe('PageValueObject', () => {
  let dateTimeValueObject: PageValueObject;

  beforeEach(() => {
    dateTimeValueObject = new PageValueObject();
  });

  it('should be defined', () => {
    expect(dateTimeValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de PageValueObject
});
