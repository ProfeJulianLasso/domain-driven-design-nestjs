import { DateTimeEndValueObject } from '.';

describe('DateTimeEndValueObject', () => {
  let dateTimeEndValueObject: DateTimeEndValueObject;

  beforeEach(() => {
    dateTimeEndValueObject = new DateTimeEndValueObject();
  });

  it('should be defined', () => {
    expect(dateTimeEndValueObject).toBeDefined();
  });

  // TODO: Falta realizar las pruebas unitarias de DateTimeEndValueObject
});
